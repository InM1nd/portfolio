Project rules live in [AGENTS.md](./AGENTS.md). Read it before changing anything.

Three rules break the project if ignored:
1. Never invent facts about the person — no metric, status or claim without a source.
2. Tabs are routes, not state. Never `ssr: false` on content.
3. The page never scrolls; the panel inside the device does. Verify by measurement.
